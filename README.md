# Bomb Defuser

Static deploy of **Bomb Defuser** (client build).

- Entry: `assets/index-CctbiIQ1.js`
- Root: `#root`

## Local

Open `index.html` via any static server:

```bash
npx serve .
```

## Deploy

- **Vercel**: root = this folder, framework = Other
- **GitHub Pages**: enable Pages on `main` / root
